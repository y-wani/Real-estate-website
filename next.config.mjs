/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['ap.rdcpix.com'],
    },

    images: {
      domains: ['dynamic-media-cdn.tripadvisor.com'], // Add this domain
    },
  };

export default nextConfig;
